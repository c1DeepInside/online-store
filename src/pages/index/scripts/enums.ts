export enum CheckboxTheme {
  categories = 'categories',
  brand = 'brand',
}

export enum FilterParams {
  priceMin = 'priceMin',
  priceMax = 'priceMax',
  stockMin = 'StockMin',
  stockMax = 'StockMax',
  search = 'search',
  view = 'view',
  sorting = 'sorting',
  brand = 'brand',
  categories = 'categories',
}
